react router gere les soulignages des liens actifs nativement, a check
Eclater App.jsx en /components/chaque component a un fichier
Le contenu d'index.jsx doit être App.jsx et gerer le routage
index doit gérer l'appel d'app qui appel le routage
créer un fichier Router.js qui va gérer les routes
usestate pour le dropdown et le carrousel
useeffect pour la gestion du fetch json qui peut 
voir la vidéo pour le dropdown > utilise le usestate pour définir le booléen à false par défaut pour l'affichage puis dans l'<img> lance une fonction OnClick qui va changer le state a true, puls qu'à changer le reste du dropdown en visible/déclencher l'animation et afficher le reste des infos du dropdown

utiliser les proptypes sur chaque component qui reçoit des infos du json

créer un fichier et son css pour chaque domponent
