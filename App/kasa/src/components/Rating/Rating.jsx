import './Rating.css';

import fullstar from '../../assets/STARCOMPLETE.svg';

export const Rating = (number) => {
    switch (number){
        case 1: break;
        default : break;
    }

    return(<div>stars</div>);
        
}